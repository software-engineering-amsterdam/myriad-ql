form testForm {
    "Did you sell a house?" ->
        hasSoldHouse  : boolean
    if (hasSoldHouse) {
        "What's the price of the house?" -> soldHousePrice : boolean
        if (hasSoldHouse || false) {
            "What's the price of the house?" -> soldHousePrice : boolean
        }
    }
}