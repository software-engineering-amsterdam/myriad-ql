form test{
    "testint" testi: integer
    "testdec" testd: decimal
    "testbool" testb: boolean
    "testdate" testda: date
    "testmoney" testm: money

    if(testb && (testi < 10) && (testd > 5.5)){
        "money result" mres: money = (testmo * 100)
    }

}